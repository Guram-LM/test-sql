import { baseTemplate, otpBox, paragraph, divider, note } from "./email.layout.js";

/* ================================================================
   OTP EMAIL
================================================================ */
export function otpEmailTemplate(otp) {
  return baseTemplate({
    title: "ანგარიშის დადასტურება",
    content: `
      ${paragraph("გამარჯობა,")}
      ${paragraph("თქვენ მოითხოვეთ ანგარიშის შექმნა. გამოიყენეთ ქვემოთ მოცემული კოდი:")}
      ${otpBox(otp)}
      ${paragraph("კოდი მოქმედია <strong>10 წუთის</strong> განმავლობაში.")}
      ${note("თუ ეს მოთხოვნა თქვენგან არ არის, უბრალოდ უგულებელყავით ეს შეტყობინება.")}
    `,
  });
}

/* ================================================================
   PASSWORD RESET
================================================================ */
export function resetEmailTemplate(code) {
  return baseTemplate({
    title: "პაროლის აღდგენა",
    content: `
      ${paragraph("გამარჯობა,")}
      ${paragraph("მივიღეთ პაროლის აღდგენის მოთხოვნა. გამოიყენეთ ეს კოდი:")}
      ${otpBox(code)}
      ${paragraph("კოდი მოქმედია <strong>15 წუთის</strong> განმავლობაში.")}
      ${note("თუ პაროლის შეცვლა არ მოგითხოვიათ, ვინმემ შეიძლება სცადა თქვენს ანგარიშზე წვდომა.")}
    `,
  });
}

/* ================================================================
   EMAIL CHANGE
================================================================ */
export function emailChangeTemplate(otp) {
  return baseTemplate({
    title: "ელფოსტის დადასტურება",
    content: `
      ${paragraph("გამარჯობა,")}
      ${paragraph("მოთხოვნილია ახალი ელფოსტა. დასადასტურებლად გამოიყენეთ ეს კოდი:")}
      ${otpBox(otp)}
      ${paragraph("კოდი მოქმედია <strong>15 წუთის</strong> განმავლობაში.")}
      ${note("თუ ეს მოთხოვნა თქვენგან არ არის, დაუკავშირდით ჩვენს გუნდს დაუყოვნებლივ.")}
    `,
  });
}